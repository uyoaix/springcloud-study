
spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:9060/
      profile: default
      label: master

server:
  port: 9070
management:
  security:
    enabled: false